export default class Neo {
  constructor(id, name, averageEstimateDiameter, isSentry) {
    this.id = id
    this.name = name
    this.averageEstimateDiameter = averageEstimateDiameter
    this.isSentry = isSentry
  }
}